Output Discipline:
- Return a single valid JSON object only.
- Do not add markdown, code fences, prose, or explanatory text outside the JSON object.
- Keep string fields concise and avoid backslashes unless they are required and properly escaped.
